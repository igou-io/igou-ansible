---
# Ship built iPXE binaries from the control node's build cache to
# flash:/netboot/ on the rb5009, then register each in /ip tftp so
# PXE clients can fetch them by their public names.
#
# Honors netboot_arches_resolved (set by netboot_build.yml). When this
# stage runs alone via --tags upload, the fact is recomputed from
# netboot_arches and the static arches map.

- name: Recompute arches map if running upload stage alone
  ansible.builtin.set_fact:
    _netboot_arches_map:
      bios:
        local_name: netboot.xyz.kpxe
        public_name: netboot.xyz.kpxe
      uefi-x64:
        local_name: netboot.xyz.efi
        public_name: netboot.xyz.efi
      uefi-arm64:
        local_name: netboot.xyz-arm64.efi
        public_name: netboot.xyz-arm64.efi
  when: netboot_arches_resolved is not defined

- name: Recompute resolved arches list if running upload stage alone
  ansible.builtin.set_fact:
    netboot_arches_resolved: >-
      {{
        netboot_arches | map('extract', _netboot_arches_map) | list
      }}
  when: netboot_arches_resolved is not defined

- name: Stat each local binary
  ansible.builtin.stat:
    path: "{{ netboot_local_build_dir }}/output/ipxe/{{ item.local_name }}"
    checksum_algorithm: sha256
  delegate_to: localhost
  loop: "{{ netboot_arches_resolved }}"
  loop_control:
    label: "{{ item.public_name }}"
  register: _netboot_local_stat

- name: Assert each local binary exists
  ansible.builtin.assert:
    that:
      - item.stat.exists
    fail_msg: >-
      Local binary
      {{ netboot_local_build_dir }}/output/ipxe/{{ item.item.local_name }}
      is missing. Run the build stage first
      (--tags build).
  loop: "{{ _netboot_local_stat.results }}"
  loop_control:
    label: "{{ item.item.public_name }}"

- name: Count flash netboot directory
  community.routeros.command:
    commands:
      - '/file print count-only where name="{{ netboot_flash_dir }}" and type=directory'
  register: _netboot_flash_dir_count
  changed_when: false

- name: Create flash netboot directory
  community.routeros.command:
    commands:
      - "/file add name={{ netboot_flash_dir }} type=directory"
  when: (_netboot_flash_dir_count.stdout[0] | trim | int) == 0
  changed_when: true

# Compare local SHA256 against a sidecar .sha256 file on rb5009 (uploaded
# alongside each binary). Size-only is not enough: iPXE outputs are
# sector-padded, so a rebuild with a different embedded boot_domain can
# produce a byte-size-identical binary whose content differs (see the
# 2026-05-11 .efi staleness incident). The :do { } on-error={ } wrapper
# treats a missing sidecar as empty content -> always uploads, which
# also recovers gracefully on first run after this fix.

- name: Read router-side SHA256 sidecar for each binary
  community.routeros.command:
    commands:
      - >-
        :do { :put [/file get
        [find where name="{{ netboot_flash_dir }}/{{ item.public_name }}.sha256"]
        contents] } on-error={ :put "" }
  register: _netboot_router_sha256
  changed_when: false
  loop: "{{ netboot_arches_resolved }}"
  loop_control:
    label: "{{ item.public_name }}.sha256"

- name: Render local SHA256 sidecar alongside each binary
  ansible.builtin.copy:
    content: "{{ item.stat.checksum }}\n"
    dest: "{{ netboot_local_build_dir }}/output/ipxe/{{ item.item.local_name }}.sha256"
    mode: "0644"
  delegate_to: localhost
  loop: "{{ _netboot_local_stat.results }}"
  loop_control:
    label: "{{ item.item.public_name }}.sha256"

- name: Upload binary when local SHA256 differs from router sidecar
  ansible.netcommon.net_put:
    src: "{{ netboot_local_build_dir }}/output/ipxe/{{ item.0.local_name }}"
    dest: "{{ netboot_flash_dir }}/{{ item.0.public_name }}"
  vars:
    ansible_command_timeout: 300
    _local_hash: "{{ item.1.stat.checksum }}"
    _router_hash: >-
      {{ (_netboot_router_sha256.results[ansible_loop.index0].stdout[0]
          | default('')) | trim }}
  when: _local_hash != _router_hash
  loop: "{{ netboot_arches_resolved | zip(_netboot_local_stat.results) | list }}"
  loop_control:
    label: "{{ item.0.public_name }}"
    extended: true
  register: _netboot_uploads

- name: Upload SHA256 sidecar after binary upload
  ansible.netcommon.net_put:
    src: "{{ netboot_local_build_dir }}/output/ipxe/{{ item.0.local_name }}.sha256"
    dest: "{{ netboot_flash_dir }}/{{ item.0.public_name }}.sha256"
  vars:
    ansible_command_timeout: 30
    _local_hash: "{{ item.1.stat.checksum }}"
    _router_hash: >-
      {{ (_netboot_router_sha256.results[ansible_loop.index0].stdout[0]
          | default('')) | trim }}
  when: _local_hash != _router_hash
  loop: "{{ netboot_arches_resolved | zip(_netboot_local_stat.results) | list }}"
  loop_control:
    label: "{{ item.0.public_name }}.sha256"
    extended: true

- name: Count tftp rows for each arch
  community.routeros.command:
    commands:
      - >-
        /ip tftp print count-only where
        req-filename="{{ item.public_name }}" and
        real-filename="{{ netboot_flash_dir }}/{{ item.public_name }}"
  register: _netboot_tftp_count
  changed_when: false
  loop: "{{ netboot_arches_resolved }}"
  loop_control:
    label: "{{ item.public_name }}"

- name: Add /ip tftp row for each arch
  community.routeros.command:
    commands:
      - >-
        /ip tftp add
        req-filename="{{ item.public_name }}"
        real-filename="{{ netboot_flash_dir }}/{{ item.public_name }}"
        allow=yes read-only=yes
  when: (_netboot_tftp_count.results[ansible_loop.index0].stdout[0] | trim | int) == 0
  changed_when: true
  loop: "{{ netboot_arches_resolved }}"
  loop_control:
    label: "{{ item.public_name }}"
    extended: true

# --- menu.ipxe TFTP redirect ----------------------------------------------
# The embedded boot script in our binaries (templates/disks/netboot.xyz.j2
# upstream) tries `tftp://${tftp-server}/menu.ipxe` first; only when that
# fails does it fall through to https/http://${boot_domain}/menu.ipxe.
# TrueNAS' netbootxyz container only exposes menu.ipxe over TFTP, so the
# https/http fallback hits "connection reset" then "404", then iPXE
# exits and the firmware drops to local boot. Serving a tiny redirect
# file from rb5009 makes the first TFTP attempt succeed; from there
# iPXE chains to the real menu on TrueNAS.

- name: Render menu.ipxe redirect locally
  ansible.builtin.copy:
    content: |
      #!ipxe
      # Generated by playbooks/routeros/tasks/netboot_upload.yml.
      # Redirects rb5009-served menu.ipxe TFTP requests to the
      # netboot.xyz menu hosted on TrueNAS, so the binaries' embedded
      # TFTP-first chainload short-circuits before the http/https
      # fallbacks (which TrueNAS does not serve).
      chain tftp://{{ netboot_chainload_host }}/menu.ipxe
    dest: "{{ netboot_local_build_dir }}/menu.ipxe"
    mode: "0644"
  delegate_to: localhost

- name: Stat local menu.ipxe redirect
  ansible.builtin.stat:
    path: "{{ netboot_local_build_dir }}/menu.ipxe"
  delegate_to: localhost
  register: _netboot_menu_local_stat

- name: Count router-side menu.ipxe matching local size
  community.routeros.command:
    commands:
      - >-
        /file print count-only where
        name="{{ netboot_flash_dir }}/menu.ipxe"
        and size={{ _netboot_menu_local_stat.stat.size }}
  register: _netboot_menu_size_match
  changed_when: false

- name: Upload menu.ipxe redirect if size differs
  ansible.netcommon.net_put:
    src: "{{ netboot_local_build_dir }}/menu.ipxe"
    dest: "{{ netboot_flash_dir }}/menu.ipxe"
  vars:
    ansible_command_timeout: 60
  when: (_netboot_menu_size_match.stdout[0] | trim | int) == 0

- name: Count tftp rows for menu.ipxe redirect
  community.routeros.command:
    commands:
      - >-
        /ip tftp print count-only where
        req-filename="menu.ipxe" and
        real-filename="{{ netboot_flash_dir }}/menu.ipxe"
  register: _netboot_menu_tftp_count
  changed_when: false

- name: Add /ip tftp row for menu.ipxe redirect
  community.routeros.command:
    commands:
      - >-
        /ip tftp add
        req-filename="menu.ipxe"
        real-filename="{{ netboot_flash_dir }}/menu.ipxe"
        allow=yes read-only=yes
  when: (_netboot_menu_tftp_count.stdout[0] | trim | int) == 0
  changed_when: true
