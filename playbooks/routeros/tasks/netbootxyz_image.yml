---
# Build the netbootxyz image tar on the control node, then upload to the
# router if size differs from what's already on flash. Sets fact
# netbootxyz_image_changed for the container stage to consume.
- name: Ensure local image cache directory exists
  ansible.builtin.file:
    path: "{{ netbootxyz_local_image_dir }}"
    state: directory
    mode: "0755"
  delegate_to: localhost

- name: Pull netbootxyz image on control node
  ansible.builtin.command: >-
    podman pull --platform={{ netbootxyz_image_platform }} {{ netbootxyz_image }}
  register: _netbootxyz_podman_pull
  delegate_to: localhost
  changed_when: "'Writing manifest' in _netbootxyz_podman_pull.stdout or 'Copying blob' in _netbootxyz_podman_pull.stdout"

- name: Stat local image tar (pre-save)
  ansible.builtin.stat:
    path: "{{ netbootxyz_local_image_dir }}/netbootxyz.tar"
  register: _netbootxyz_local_tar_pre
  delegate_to: localhost

# noqa: no-handler — keep linear; downstream stat/count/net_put need the
# tar in place before the play ends, when handlers would normally flush.
- name: Save image to tar (when pull changed or local tar missing)  # noqa: no-handler
  ansible.builtin.command: >-
    podman save {{ netbootxyz_image }}
    -o {{ netbootxyz_local_image_dir }}/netbootxyz.tar
  delegate_to: localhost
  when: _netbootxyz_podman_pull.changed or not _netbootxyz_local_tar_pre.stat.exists
  changed_when: true

- name: Stat local image tar
  ansible.builtin.stat:
    path: "{{ netbootxyz_local_image_dir }}/netbootxyz.tar"
  register: _netbootxyz_local_tar
  delegate_to: localhost

- name: Fail if local tar still missing after save attempt
  ansible.builtin.assert:
    that:
      - _netbootxyz_local_tar.stat.exists
    fail_msg: >-
      {{ netbootxyz_local_image_dir }}/netbootxyz.tar does not exist on the
      control node even after the save step. Inspect the podman pull output
      (the prior task) for errors. If the image is corrupted in the local
      podman cache, run `podman rmi {{ netbootxyz_image }}` and re-run.

- name: Count router-side image tars matching local size
  community.routeros.command:
    commands:
      - "/file print count-only where name=\"{{ netbootxyz_root_dir }}/image.tar\" and size={{ _netbootxyz_local_tar.stat.size }}"
  register: _netbootxyz_router_tar_match
  changed_when: false

- name: Decide whether to upload image tar
  ansible.builtin.set_fact:
    _netbootxyz_need_upload: "{{ (_netbootxyz_router_tar_match.stdout[0] | trim | int) == 0 }}"

- name: Upload image tar to router
  ansible.netcommon.net_put:
    src: "{{ netbootxyz_local_image_dir }}/netbootxyz.tar"
    dest: "{{ netbootxyz_root_dir }}/image.tar"
  vars:
    ansible_command_timeout: 600
  when: _netbootxyz_need_upload
  register: _netbootxyz_net_put_result

- name: Set netbootxyz_image_changed fact
  ansible.builtin.set_fact:
    netbootxyz_image_changed: "{{ _netbootxyz_need_upload | bool }}"
