---
galaxy_info:
  role_name: rustfs_state
  author: david-igou
  description: >-
    Declarative in-server state management for RustFS instances via the
    pinned rc CLI: IAM policies, buckets, versioning, lifecycle rules,
    users, policy attachments, and credential liveness verification, with
    check mode as drift detection. Pure over its inputs — no secret
    lookups inside the role.
  license: MIT
  min_ansible_version: "2.15"
  galaxy_tags:
    - rustfs
    - s3
    - storage
dependencies: []
