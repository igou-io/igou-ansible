---
# This EE intentionally has legacy security policies to communicate with legacy devices.
version: 3
images:
  base_image:
    # renovate: datasource=docker depName=quay.io/centos/centos
    name: quay.io/centos/centos:stream9-minimal
options:
  package_manager_path: /usr/bin/microdnf
dependencies:
  python_interpreter:
    package_system: python3.12
    python_path: /usr/bin/python3.12
  ansible_core:
    # renovate: datasource=pypi depName=ansible-core
    package_pip: ansible-core==2.21.0
  ansible_runner:
    # renovate: datasource=pypi depName=ansible-runner
    package_pip: ansible-runner==2.4.3
  galaxy:
    collections:
      - name: community.general
        version: 10.2.0
      - name: ansible.netcommon
        version: 7.1.0
      - name: community.network
        version: 5.1.0
      - name: vyos.vyos
        version: 5.0.0
      - name: arista.eos
        version: 10.0.1
      - name: community.routeros
        version: 2.18.0
  system: |
    python3.12-devel [platform:rpm compile]
    libcurl-devel [platform:rpm compile]
    krb5-devel [platform:rpm compile]
    krb5-workstation [platform:rpm]
    subversion [platform:rpm]
    subversion [platform:dpkg]
    git-lfs [platform:rpm]
    sshpass [platform:rpm]
    rsync [platform:rpm]
    epel-release [platform:rpm]
    unzip [platform:rpm]
    podman-remote [platform:rpm]
    cmake [platform:rpm compile]
    gcc [platform:rpm compile]
    gcc-c++ [platform:rpm compile]
    make [platform:rpm compile]
    openssl-devel [platform:rpm compile]
    1password-cli
    crypto-policies-scripts [platform:rpm]
    tar
  python: requirements.txt
additional_build_files:
  - dest: configs
    src: custom.conf
additional_build_steps:
  prepend_base: |
    RUN microdnf install -y git-core
  append_base: |
    RUN $PYCMD -m pip install -U pip

    RUN rpm --import https://downloads.1password.com/linux/keys/1password.asc && \
    sh -c 'echo -e "[1password]\nname=1Password Stable Channel\nbaseurl=https://downloads.1password.com/linux/rpm/stable/\$basearch\nenabled=1\ngpgcheck=1\nrepo_gpgcheck=1\ngpgkey=https://downloads.1password.com/linux/keys/1password.asc" > /etc/yum.repos.d/1password.repo'

  append_final: |
    COPY _build/configs/custom.conf /etc/ssh/ssh_config.d/custom.conf
    RUN chmod 600 /etc/ssh/ssh_config.d/custom.conf
    RUN rm /etc/ssh/ssh_config.d/50-redhat.conf
    # renovate: datasource=github-releases depName=ansible/receptor
    ARG RECEPTOR_VERSION="v1.6.5"
    RUN curl -L -o /tmp/receptor.tar.gz "https://github.com/ansible/receptor/releases/download/${RECEPTOR_VERSION}/receptor_${RECEPTOR_VERSION#v}_linux_amd64.tar.gz" && \
        tar xzf /tmp/receptor.tar.gz -C /usr/bin/ receptor && \
        chmod +x /usr/bin/receptor && \
        rm -f /tmp/receptor.tar.gz
    RUN mkdir -p /var/run/receptor && \
        git lfs install --system && \
        alternatives --install /usr/bin/python python /usr/bin/python3.12 312
    RUN update-crypto-policies --set LEGACY && \
        update-crypto-policies --set DEFAULT:SHA1
    RUN ansible --version && ansible-galaxy collection list
