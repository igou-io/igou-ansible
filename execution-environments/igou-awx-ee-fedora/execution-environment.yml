---
version: 3
images:
  base_image:
    name: quay.io/fedora/fedora:latest
options:
  package_manager_path: /usr/bin/dnf
dependencies:
  python_interpreter:
    package_system: python3.14
    python_path: /usr/bin/python3.14
  ansible_core:
    # Require minimum of 2.15 to get ansible-inventory --limit option
    package_pip: ansible-core
  ansible_runner:
    package_pip: ansible-runner
  galaxy: ../../requirements.yml
  system: |
    python3.14-devel [platform:rpm compile]
    libcurl-devel [platform:rpm compile]
    krb5-devel [platform:rpm compile]
    krb5-workstation [platform:rpm]
    subversion [platform:rpm]
    subversion [platform:dpkg]
    git-lfs [platform:rpm]
    sshpass [platform:rpm]
    rsync [platform:rpm]
    unzip [platform:rpm]
    podman-remote [platform:rpm]
    cmake [platform:rpm compile]
    gcc [platform:rpm compile]
    gcc-c++ [platform:rpm compile]
    make [platform:rpm compile]
    openssl-devel [platform:rpm compile]
    1password-cli
    terraform
    coreos-installer
    cargo
    tar
    helm
  python: requirements.txt
additional_build_steps:
  prepend_base: |
    RUN dnf install -y git-core
  append_base: |
    RUN $PYCMD -m pip install -U pip

    # Configure Terraform Repo
    RUN curl -o /etc/yum.repos.d/hashicorp.repo https://rpm.releases.hashicorp.com/fedora/hashicorp.repo

    RUN rpm --import https://downloads.1password.com/linux/keys/1password.asc && \
    sh -c 'echo -e "[1password]\nname=1Password Stable Channel\nbaseurl=https://downloads.1password.com/linux/rpm/stable/\$basearch\nenabled=1\ngpgcheck=1\nrepo_gpgcheck=1\ngpgkey=https://downloads.1password.com/linux/keys/1password.asc" > /etc/yum.repos.d/1password.repo'

  append_final: |
    COPY --from=quay.io/ansible/receptor:devel /usr/bin/receptor /usr/bin/receptor
    RUN mkdir -p /var/run/receptor && \
        git lfs install --system && \
        alternatives --install /usr/bin/python python /usr/bin/python3.14 314
    RUN curl -L -o /tmp/kustomize.tar.gz https://github.com/kubernetes-sigs/kustomize/releases/download/kustomize/v5.4.3/kustomize_v5.4.3_linux_amd64.tar.gz && \
        tar xzf /tmp/kustomize.tar.gz -C /usr/local/bin/ && \
        chmod +x /usr/local/bin/kustomize && \
        rm -f /tmp/kustomize.tar.gz
    RUN python -V && ansible --version && ansible-galaxy collection list
